{
  "name": "observe-mais-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/gerar-blog.mjs && node scripts/build.mjs",
    "check": "npm test && npm run build",
    "dev": "node scripts/serve.mjs",
    "blog": "node scripts/gerar-blog.mjs"
  }
}
